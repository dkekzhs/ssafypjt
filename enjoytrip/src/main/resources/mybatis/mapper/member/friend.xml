<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.web.friend.model.mapper.FriendMapper">


    <insert id="add" parameterType="friendAddDto">
        insert into friend(`from`,`to`) values( #{from} , #{to} );
    </insert>

    <select id="friendRequestPending" parameterType="String" resultType="friendPendingDto">
        select `to` ,`from` from friend where `to` =#{user_id} and status = 0
    </select>

    <update id="accept" parameterType="friendAddDto">
        update friend set status =1 where `from`=#{from} and `to`=#{to};
    </update>

<select id="findFriends" parameterType="String" resultType="friendAddDto">
    SELECT DISTINCT
        CASE
            WHEN `from` = #{id} THEN f.`to`
            WHEN `to` = #{id} THEN f.`from`
        END AS `to`
    FROM friend f
    WHERE (`from` = #{id} OR `to` = #{id}) AND status = 1;
</select>

    <select id="friendCount" parameterType="String" resultType="int">
        select count(*) from friend where `to` =#{user_id} and status = 0
    </select>

    <delete id="refuse" parameterType="friendAddDto">
        DELETE FROM friend
        WHERE `from` = #{from} AND `to` = #{to} AND status = 0
    </delete>
</mapper>
